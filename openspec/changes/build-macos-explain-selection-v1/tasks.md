Completed items below retain credit for their implemented scope. Legacy task 2.4 and clipboard-specific portions of 5.1 are transitional implementation records, superseded by the clipboard-free target. The original development matrix task moved to 2.12; the original combined installer task was split into section 6. The previous section 6 Developer ID/notarization tasks (old 6.2–6.4) are superseded by the zero-paid-Apple direct-distribution decision, not unfinished blockers. Old 6.5–6.6 are replaced by the updater and publication tasks below. See the implementation-gap table in `design.md`.

## 1. Foundation and feasibility

- [x] 1.1 Scaffold a macOS Tauri 2 app with a small TypeScript frontend, native Rust command boundary, and local development workflow.
- [x] 1.2 Prove that a registered global shortcut can identify the frontmost app and read selected text through macOS Accessibility before opening a Tauri window; document any Tauri/AppKit blocker before proceeding.
- [ ] 1.3 Decide and document the minimum supported macOS version, supported CPU architectures, configured default OpenAI model identifier, and supported-app acceptance criteria. Record current candidates (macOS 14.0, arm64, `gpt-4.1-mini`) separately from verified support; require minimum-platform evidence and the live native model result from 3.7 before claiming a tested default.
- [x] 1.4 Define and document the selection-capture/privacy policy: the legacy automated Copy fallback once accessed the general pasteboard, but the final capture flow never reads or modifies it; specify resolver ordering, verified semantic versus visually recovered/unverified methods, progressive Screen Recording permission, concrete time/text/pixel-area limits, deterministic reading order, and local-only ephemeral screenshot/OCR handling. Prohibit screenshot transmission to the LLM/network unless a future feature explicitly changes that requirement; distinguish selected-text OCR from separately enabled surrounding visual context.

## 2. Selection capture

- [x] 2.1 Add shortcut registration, conflict reporting, remapping, and supersession of older invocations.
- [x] 2.2 Implement a bounded native AX selection reader with focused-app identity, range support, explicit empty-selection detection, and messaging timeout.
- [x] 2.3 Add secure-field and excluded-app checks before any selection or context read.
- [x] 2.4 Legacy implementation only: serialized automated Copy fallback with fresh pasteboard-change detection and best-effort guarded restoration existed. It was deprecated and retired under 2.13; it is not a target product requirement.
- [x] 2.5 Implement the existing typed verified/unverified/empty/blocked/oversized/timed-out/failed capture states and worker execution. The expanded provenance, geometry, confidence, and permission contract remains pending under 2.6.
- [x] 2.6 Extend the native/Rust/frontend capture-result model with selected text, source application, explicit provider-authored capture method/provenance, confidence/verification state, optional AX range/bounds, and verified, visually recovered/unverified, empty, blocked, oversized, timed-out, permission-required (with permission kind), and failed outcomes. Stop inferring provenance from detail strings and keep capture work off the UI thread.
- [x] 2.7 Add a layered `SelectionResolver` that tries direct AX selected text, then AX selected range plus string-for-range, reusing existing AX primitives. Preserve secure-field/exclusion checks, authoritative empty-selection handling, source identity, deadlines, and supersession; do not let terminal outcomes silently fall through to weaker providers.
- [x] 2.8 Recover and validate AX selection geometry/bounds when a nonempty selection can be located but its text cannot be read; handle visible coordinates/display scale and reject stale, excessive, or unrelated regions before image acquisition.
- [x] 2.9 Implement shared bounded local OCR for only the necessary selection region(s), with documented timeout and maximum pixel area, deterministic reading-order reconstruction, text-size limits, cancellation/supersession, and image-buffer disposal. Keep screenshots out of network payloads, webview IPC, files, history, and logs.
- [x] 2.10 Add an explicit “Drag to explain” action/overlay only after semantic recovery fails and automatic visual recovery is unavailable or unusable. OCR the user-drawn region locally, preserve source/secure-field/exclusion checks, support cancellation, and return visually recovered/unverified text with provenance/confidence. Do not add passive global mouse-drag inference.
- [x] 2.11 Integrate AX-bounds OCR and explicit visual selection into resolver ordering after semantic paths, with progressive permission handling from 4.5, off-UI-thread processing, terminal failure behavior, and no automatic widening of captured regions.
- [ ] 2.12 Run a development matrix for Safari, Chrome, TextEdit, Notes, Preview selectable PDFs, and VS Code across implemented paths; record exact expected/actual selected text, latency, capture method, confidence/verification state, permission requirements, false positives, and empty-selection behavior. Mark unavailable paths explicitly; this is development evidence, not the final fresh-profile acceptance run.
- [x] 2.13 After replacement paths pass focused tests and development validation (or earlier only with documented evidence that removal is safe), retire automated Copy and all general-pasteboard reads/writes from the shipped capture flow. Retain clipboard regression tests while legacy code exists, then retire obsolete race/restoration tests and verify the final no-clipboard-access invariant.

## 3. Explanation flow and credentials

- [x] 3.1 Implement Keychain-backed save, replace, read-for-native-request, and remove actions for the user's OpenAI API key; clear the settings input after saving and never return the saved key to the webview.
- [x] 3.2 Implement the three versioned preset prompts and bounded optional context from the same AX text provider. Extension of the context policy to visual capture remains pending under 3.6.
- [x] 3.3 Implement native OpenAI Responses API requests with `store: false`, no tools, output/request limits, cancellation, and provider error mapping.
- [x] 3.4 Build a floating panel that loads after capture, auto-sends verified text using Explain Simply, previews unverified text before sending, and reuses the snapshot for alternate presets. Visual-capture labels and confidence remain pending under 3.8.
- [x] 3.5 Handle missing keys, denied permissions, empty selections, network failures, invalid models, and rate limits without accidental sends or stale replies.
- [x] 3.6 Enforce the revised context-sharing policy across all capture methods: semantic surrounding context is opt-in, bounded, and available only from the semantic provider; selected-text OCR never authorizes surrounding-screen OCR. Keep surrounding visual context disabled in v1 and require separate explicit enablement if a future feature adds it. Keep screenshot data out of the text-only native request boundary and preserve snapshot reuse.
- [ ] 3.7 Add and document a developer-only, explicitly invoked live-provider smoke test using the real native Responses client and configured default model, with locally supplied credentials held only as needed and no unnecessary persistence. Keep it outside normal mocked CI; document the manual/pre-release command, harmless fixture, exact model, pass criteria, and reviewed live result without logging credentials or silently substituting models.
- [x] 3.8 Extend the existing panel to show AX/OCR provenance, confidence/verification, and permission-required recovery. Require explicit confirmation of all visually recovered/unverified text, retain its provenance after confirmation, and keep retries/preset switches on the frozen snapshot without new capture.

## 4. Local history and controls

- [x] 4.1 Create an app-owned SQLite schema and migrations for successful explanation records, excluding API credentials.
- [x] 4.2 Add local history listing, search, individual deletion, clear-all, and disable-new-history behavior.
- [x] 4.3 Build first-run permission/key guidance and settings for shortcut, context sharing, excluded apps, history, and model override.
- [x] 4.4 Review IPC permissions and logs so captured text, context, API key, and responses are exposed only to the commands and UI that need them.
- [x] 4.5 Extend onboarding for progressive permissions: Accessibility guidance for semantic capture; request Screen Recording only when visual/OCR capture is needed or explicitly enabled. Do not prompt AX-only users for Screen Recording on first launch, and keep permitted AX paths usable after Screen Recording denial.
- [x] 4.6 Extend the IPC/history/logging review to visual capture: expose only required text/provenance/permission metadata, persist the actual OCR capture method with successful explanations, and keep screenshots and credentials out of IPC, history, and logs. Preserve existing settings and history records.

## 5. Product verification

- [x] 5.1 Add the existing focused tests for no-selection status gates, capture supersession, prompt snapshot reuse, key isolation, and history deletion, plus legacy stale-clipboard/change-count guards. The latter are transitional regression coverage, not final product acceptance; the existing tests do not establish resolver/OCR compliance.
- [x] 5.2 Add focused resolver tests for no-selection false positives (including authoritative empty ranges and stale inactive-window selections), selected-range/string-for-range fallback, provider ordering and stop conditions, and typed provenance/verification/confidence propagation.
- [x] 5.3 Add focused geometry/OCR tests for timeout, maximum pixel area before acquisition, minimal-region isolation, deterministic reading order, text limits, cancellation, and capture supersession through both automatic bounds OCR and explicit visual selection.
- [x] 5.4 Test the screenshot/network privacy boundary and context policy: outbound request content contains only permitted text, never screenshot bytes, encoded images, or screenshot paths/URLs; selected-region OCR and semantic-context opt-in cannot cause surrounding-screen OCR. Verify screenshot buffer disposal and no image persistence/exposure.
- [x] 5.5 Extend regression coverage across new paths for secure fields/excluded apps, progressive Accessibility/Screen Recording permission and denial, explicit visual action/confirmation, prompt snapshot reuse, key isolation, and history deletion. Keep normal CI mocked and credential-free; use 3.7 for real-provider validation.
- [x] 5.6 After 2.13, verify every final capture path performs zero general-pasteboard reads/writes or automated Copy, including empty, denied, cancelled, superseded, and failed paths. Clipboard race-condition behavior is no longer an acceptance criterion after retirement.
- [ ] 5.7 Run one final supported-app acceptance matrix on a fresh macOS user profile after the clipboard-free paths are implemented: Safari, Chrome, TextEdit, Notes, Preview selectable PDFs, and VS Code; direct AX, AX range fallback, visual fallback where applicable, empty selections, secure fields, permission denial, long selections, selectable PDFs, shortcut conflicts, and floating-panel visibility/positioning. Record exact text, latency, method, confidence/verification, required permissions, and false positives. Narrow README/support/privacy claims whenever an app/control/path is unreliable; replace obsolete Copy-based target claims and preserve measured limitations.

## 6. Direct distribution

- [x] 6.1 Produce an ad-hoc-signed macOS `.app` and DMG suitable for direct public download; verify their expected architecture, app identity, and package contents. Retain the recorded development DMG readability check as partial evidence only.
- [ ] 6.2 Download the release DMG on fresh user profiles for the supported minimum and current macOS versions; mount it, drag Splainit into `/Applications`, attempt first launch, record the exact Gatekeeper warning if any, approve only this app through System Settings → Privacy & Security when offered, and verify a successful subsequent launch. Do not claim a warning-free install.
- [ ] 6.3 Review and test installation instructions so normal installation never requires globally disabling Gatekeeper, `spctl --master-disable`, manually removing quarantine, root execution, or other system-wide security weakening. Distinguish expected per-app approval from a damaged/altered artifact that requires investigation.
- [ ] 6.4 Verify first-run onboarding after direct installation: Accessibility permission, Screen Recording only when visual/OCR fallback needs it, user API key entry, shortcut configuration, settings persistence, and app restart behavior.
- [x] 6.5 Generate an independent Tauri updater signing keypair; embed only the public key in the app, protect and back up the private key as release-critical infrastructure, and document explicit key-loss and rotation/migration procedures.
- [x] 6.6 Configure a static Tauri update endpoint using GitHub Releases unless an existing suitable release host is documented; verify update metadata points to the intended release artifacts and signatures.
- [x] 6.7 Automate tag-triggered release CI to build and ad-hoc-sign the app, build the DMG and updater artifact, sign the updater artifact with the protected Tauri private key, generate update metadata and checksums, and publish the DMG, updater artifact, signature, and `latest.json` through GitHub Releases. Use protected updater/publication secrets; require no paid Apple signing/notarization or live OpenAI credential, and keep secrets out of source, logs, caches, and public artifacts.
- [ ] 6.8 Test a real upgrade from a previous Splainit release, reject an invalid updater signature, and verify retention of settings, Keychain-stored OpenAI key access, local SQLite history, shortcut configuration, and applicable Accessibility/Screen Recording permission behavior; document rollback/schema compatibility.
- [ ] 6.9 Publish installation documentation explaining the unidentified/unverified-developer warning, absence of Apple Developer Program participation, and the supported per-app System Settings → Privacy & Security approval flow. State that users must never disable Gatekeeper globally; describe observed behavior on the supported minimum and current macOS versions.
- [ ] 6.10 Add release-integrity tooling and documentation: publish SHA-256 checksums for DMGs, verify Tauri updater signatures against the embedded public key, and document the build process and artifact verification. If source is public, link release artifacts to the source repository as an optional trust aid.
